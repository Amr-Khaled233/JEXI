services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: jexi
      POSTGRES_PASSWORD: jexi
      POSTGRES_DB: jexi
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - jexi-db:/var/lib/postgresql/data

volumes:
  jexi-db:
